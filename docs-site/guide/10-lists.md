# 10. Lists

TODO: this chapter will cover the six built-in data lists (`L1`-`L6`),
the spreadsheet-style list editor, named lists (2-5 character names),
list-valued expressions (`{1,2,3}`, `range(...)`, element-wise
arithmetic), and list functions (`sum`, `mean`, `median`, `stdev`,
`sort_asc`, `cumsum`, and more).
