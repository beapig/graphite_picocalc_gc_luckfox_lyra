# 8. Parametric, polar, and sequence modes

TODO: this chapter will cover the three non-function graph modes:
parametric (`X1T(t)`, `Y1T(t)` pairs over `Tmin..Tmax`), polar (`r(theta)`
over `THmin..THmax`, angle-mode aware), and sequence (`u`/`v`/`w`, with
cross-referencing, TIME and WEB/cobweb plot styles).
