# 6. Tracing and the CALC menu

TODO: this chapter will cover `TRACE` (mode-aware readout: `x`/`y`, `t`,
or `theta`) and the graph-screen CALC menu: value, zero, minimum,
maximum, intersect, numeric derivative (`dy/dx`), and numeric integral,
across function/parametric/polar modes.
