# 2. Variables and storage

TODO: this chapter will cover the A-Z and `theta` variables, the `->`
store operator (e.g. `2->A`), `Ans` as the last-result variable, and how
variables persist to the SD card across power cycles.
