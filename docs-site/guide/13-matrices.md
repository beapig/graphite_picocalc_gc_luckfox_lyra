# 13. Matrices

TODO: this chapter will cover the ten matrix variables (`[A]`-`[J]`),
home-screen matrix literals, arithmetic, determinant, inverse,
transpose, row-echelon forms (`rref`/`ref`), rank, eigenvalues,
eigenvectors, and the numeric equation solver.
