# 7. Tables

TODO: this chapter will cover the value table (mode-aware columns),
AUTO mode (infinite scroll in both directions) versus ASK mode
(typed/accumulated values), table setup (start/step), and column
scrolling.
