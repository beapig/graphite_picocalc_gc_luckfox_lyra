# 3. Functions and the catalog

TODO: this chapter will cover the built-in function set (trig,
logarithms, combinatorics, rounding, and more) and the built-in `F5`
HELP catalog browser, which lists every function from the same table
the expression parser registers from. See the generated
[Function catalog](../reference/function-catalog.md) for the full,
always-current list.
