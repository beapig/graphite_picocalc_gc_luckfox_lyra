# 5. Window and zoom

TODO: this chapter will cover the WINDOW screen (Xmin/Xmax/Ymin/Ymax and
the mode-aware `Tmin`/`Tmax`/`Tstep`/`THmin`/`THmax`/`THstep` fields),
and the zoom family: zoom in/out, ZStandard, ZTrig, ZBox, ZDecimal,
ZSquare, ZoomFit, and ZoomStat.
