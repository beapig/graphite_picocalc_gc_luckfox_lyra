# 1. Basic calculations

TODO: this chapter will cover entering expressions on the home screen,
the natural math display (stacked fractions, raised exponents,
auto-scaling parentheses), `Ans`, `UP`/`DOWN` history recall and
`Alt+UP`/`Alt+DOWN` history scrolling, and the FLOAT/FIX/SCI/ENG display
formats set from the MODE screen.
