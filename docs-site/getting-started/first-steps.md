# First steps

TODO: a walkthrough of the home screen for someone who just flashed the
firmware and is looking at it for the first time — entering an
expression, `ENTER` to evaluate, recalling history with `UP`/`DOWN`,
storing a value with `2->A`, and a tour of the six home-screen softkeys
(`F1` Y= editor, `F2` window, `F3` graph, `F4` mode, `F5` help, `F6`
hardware diagnostics). Should also point out the built-in help browser
(`F5` from Home) as a live, always-current reference that complements
this guide.

Source material to draw from when writing this: the main repository's
`README.md` "Using the calculator" section, and a hands-on pass with the
device or a future emulator build.
